(function () { // store vars in a privately scoped anonymous function

    //var testyTest = null;


    /* when DOM is ready, bind some shit */

    $(document).ready(function() {

		//testyTest = $('input[type="radio"]');

    });

    /* /when DOM is ready, bind some shit */

})();